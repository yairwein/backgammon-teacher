/**
 * Move generation utilities — re-exports from rules.ts for the
 * module boundary specified in the architecture.
 */
export { generateLegalMoves, isMoveLegal, applyMove, formatMove } from './rules.js';
